import React from "react";
import { Button } from "./ui/button";
import { Switch } from "./ui/switch";

const Settings = () => {
  return (
    <section className="flex flex-col items-center justify-centere h-full text-slate-950 dark:text-slate-50">
      {/* <fieldset>
        <legend className="text-lg font-semibold mb-4">Mode</legend>
        <Switch />
      </fieldset> */}
      <h1 className="text-2xl font-bold mb-4">Coming Soon!</h1>
    </section>
  );
};

export default Settings;
